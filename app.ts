function add(n1: number, n2: number, showRes: boolean, resultIs : string){
  if (showRes) {
    console.log(message, n1 + n2);
  }
}

const number1 = 2.9;
const number2 = 33;
const printRes = true;
const message = 'result is: '

add(number1, number2, printRes, message);
